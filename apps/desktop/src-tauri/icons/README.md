# Icons

`tauri icon <source.png>` 로 생성한다. 최소 요구 크기는 1024×1024 PNG. 생성되는 파일 목록:

- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)
- Android / iOS 용 리소스 (mobile 확장 시)

실제 아이콘 자산은 Phase 0 후반에 디자인되면 교체한다.
